import Layout from "~/layout";
import { Typography, Button } from "@NovaOrg/nova-mfe-shared-ui";

const Signin = () => {
  return (
    <Layout>
      <Typography>Signin page</Typography>
    </Layout>
  );
};

export default Signin;
